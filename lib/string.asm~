[SECTION .text]

global		memcpy

; void* memcpy(void* pDst, void* pSrc, int iSize)
memcpy:
	push	ebp
	mov		ebp,esp
	push	ecx
	push	esi
	push	edi

	mov		ecx,[esp + 16]
	mov		esi,[esp + 12]
	mov		edi,[esp + 8]
	cld		
	xchg	bx,bx
	rep		movsb

	pop		edi
	pop		esi
	pop		ecx
	pop		ebp
ret
